# API pública do ADVeyes

O contrato OpenAPI canônico está em [`openapi.yaml`](openapi.yaml).
A documentação interativa fica disponível em `/api/docs/` e a especificação
para geração de clientes em `/api/openapi.yaml`.
